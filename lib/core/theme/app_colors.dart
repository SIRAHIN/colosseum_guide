import 'package:flutter/material.dart';

/// Ancient Roman inspired color palette — warm earths, bronze, parchment.
class AppColors {
  AppColors._();

  // ── Backgrounds ──
  static const Color background = Color(0xFF12100E);
  static const Color surface = Color(0xFF1E1B17);
  static const Color surfaceHigh = Color(0xFF2A2520);
  static const Color surfaceHighest = Color(0xFF342F28);

  // ── Primary — Gold / Bronze ──
  static const Color gold = Color(0xFFD4A853);
  static const Color goldLight = Color(0xFFE8C878);
  static const Color goldDark = Color(0xFF8B6914);
  static const Color bronze = Color(0xFFB87333);

  // ── Accent ──
  static const Color terracotta = Color(0xFFC45B3E);
  static const Color marble = Color(0xFFE8E0D4);

  // ── Text ──
  static const Color textPrimary = Color(0xFFF5F0E8);
  static const Color textSecondary = Color(0xFFA89F91);
  static const Color textDisabled = Color(0xFF6B635A);

  // ── Borders / Dividers ──
  static const Color border = Color(0xFF3D3730);
  static const Color borderLight = Color(0xFF4A4239);
  static const Color divider = Color(0xFF2E2923);

  // ── Overlays ──
  static const Color overlay = Color(0x99000000);
  static const Color overlayLight = Color(0x4D000000);

  // ── Status ──
  static const Color success = Color(0xFF7BA05B);
  static const Color warning = Color(0xFFD4A853);
  static const Color error = Color(0xFFC45B3E);
}
